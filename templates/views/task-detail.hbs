<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css"
        integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4" crossorigin="anonymous">
    
    <title>Calendar</title>
</head>
<body>
    {{>header}}

    <main id="content">
        <div class="container col-sm-8 col-md-8 col-lg-8 mt-5">
            <div class="card">
                <h3 class="card-header">Task <a href={{task.url}}>{{task.title}}</a></h3>
                <div>
                    <p><h4>Description:</h4> {{task.description}}</p>
                    <p><h4>Date:</h4> {{task.date}}</p>
                    <p><h4>Completed:</h4> {{task.completed}}</p>
                    <p><h4>Priority:</h4> {{task.priority}}</p>
                    <p><h4>Reminders:</h4>
                        {{#each reminders}}
                            <a href={{this.url}}><b>{{this.title}}</b></a> {{#if this.read}} is read {{else}} isn't read {{/if}}<br>
                        {{/each}}
                    </p>
                </div>  
                <hr>
                <div class="form-inline">
                    <a  class="btn btn-outline-primary col-sm-4" href={{task.url}}/delete>Delete task</a> 
                    <a  class="btn btn-outline-primary col-sm-4" href={{task.url}}/update>Update task</a> 
                    <a  class="btn btn-outline-primary col-sm-4" href={{task.url}}/complete>Complete task</a>
                </div>
            </div>
        </div>
    </main>

    {{>footer}}
</body>
</html>